{
  "name": "youtube-ui-lib",
  "version": "1.0.0-beta2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "gulp sass",
    "dev": "gulp sass:watch",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-sass": "^4.0.2",
    "node-sass": "^4.12.0"
  }
}